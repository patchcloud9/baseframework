# Example .env file
# Copy this to .env and fill in production values. Never commit your real .env to source control.

APP_NAME="Base Framework"
APP_ENV=development
APP_DEBUG=true
APP_URL="http://localhost"
APP_TIMEZONE=America/Los_Angeles

DB_HOST=localhost
DB_NAME=baseframework
DB_USER=root
DB_PASS=changeme
